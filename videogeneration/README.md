# 🎞 Video Generation - AnimateDiff (Stub)

Converts generated image to video using AnimateDiff (placeholder in this repo).

## Run

```bash
python animate.py "your prompt"
```

> Output saved to `output/video.mp4`
